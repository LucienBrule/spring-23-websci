apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{.Release.Name}}-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{.Release.Name}}-api
  template:
    metadata:
      labels:
        app: {{.Release.Name}}-api
      annotations:
        seccomp.security.alpha.kubernetes.io/pod: runtime/default  # or localhost
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault  # or Localhost
      imagePullSecrets:
        - name: {{.Values.global.imagePullSecret}}
      containers:
        - name: {{.Release.Name}}-api
          image: "{{include "chart.api.image.url" . }}"
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "8080"
          command: [ "yarn" ]
          args: [ "start" ]
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
